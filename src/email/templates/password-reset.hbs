<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{#if isVietnamese}}Đặt lại mật khẩu{{else}}Reset Password{{/if}}</title>
    <style>
        body { 
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif; 
            line-height: 1.6; 
            color: #333;
            margin: 0;
            padding: 0;
            background-color: #f4f4f4;
        }
        .container { 
            max-width: 600px; 
            margin: 0 auto; 
            padding: 20px;
            background-color: #ffffff;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .header {
            text-align: center;
            padding: 20px 0;
            border-bottom: 2px solid #dc3545;
            margin-bottom: 30px;
        }
        .logo {
            font-size: 24px;
            font-weight: bold;
            color: #dc3545;
        }
        .content {
            padding: 0 20px;
        }
        h1 {
            color: #dc3545;
            text-align: center;
        }
        .button { 
            display: inline-block; 
            padding: 15px 30px; 
            background-color: #dc3545; 
            color: white !important; 
            text-decoration: none; 
            border-radius: 5px;
            font-weight: bold;
            text-align: center;
            margin: 20px 0;
        }
        .button:hover {
            background-color: #c82333;
        }
        .button-container {
            text-align: center;
        }
        .warning {
            background-color: #fff3cd;
            border: 1px solid #ffeaa7;
            border-radius: 5px;
            padding: 15px;
            margin: 20px 0;
        }
        .warning-icon {
            color: #856404;
            font-weight: bold;
        }
        .footer { 
            margin-top: 40px; 
            padding-top: 20px;
            border-top: 1px solid #eee;
            font-size: 12px; 
            color: #666; 
            text-align: center;
        }
        .url-fallback {
            word-break: break-all;
            color: #dc3545;
            margin: 10px 0;
        }
        @media only screen and (max-width: 600px) {
            .container {
                margin: 10px;
                padding: 15px;
            }
            .content {
                padding: 0 10px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <div class="logo">🧳 TripMaster</div>
        </div>
        
        <div class="content">
            {{#if isVietnamese}}
                <h1>Xin chào {{firstName}}!</h1>
                <p>Bạn đã yêu cầu đặt lại mật khẩu cho tài khoản TripMaster của mình.</p>
                <p>Để tạo mật khẩu mới, vui lòng nhấp vào nút bên dưới:</p>
                
                <div class="button-container">
                    <a href="{{resetUrl}}" class="button">🔑 Đặt lại mật khẩu</a>
                </div>
                
                <p>Nếu nút không hoạt động, hãy sao chép và dán liên kết sau vào trình duyệt của bạn:</p>
                <div class="url-fallback">{{resetUrl}}</div>
                
                <div class="warning">
                    <p class="warning-icon">⚠️ Quan trọng:</p>
                    <ul>
                        <li>Liên kết này sẽ hết hạn sau <strong>1 giờ</strong> vì lý do bảo mật</li>
                        <li>Chỉ sử dụng liên kết này nếu bạn đã yêu cầu đặt lại mật khẩu</li>
                        <li>Không chia sẻ liên kết này với ai khác</li>
                    </ul>
                </div>
                
                <p>Nếu bạn không yêu cầu đặt lại mật khẩu, vui lòng bỏ qua email này. Tài khoản của bạn vẫn an toàn.</p>
                <p>Nếu bạn tiếp tục gặp vấn đề, vui lòng liên hệ với đội ngũ hỗ trợ của chúng tôi.</p>
            {{else}}
                <h1>Hello {{firstName}}!</h1>
                <p>You have requested to reset your TripMaster account password.</p>
                <p>To create a new password, please click the button below:</p>
                
                <div class="button-container">
                    <a href="{{resetUrl}}" class="button">🔑 Reset Password</a>
                </div>
                
                <p>If the button doesn't work, copy and paste this link into your browser:</p>
                <div class="url-fallback">{{resetUrl}}</div>
                
                <div class="warning">
                    <p class="warning-icon">⚠️ Important:</p>
                    <ul>
                        <li>This link will expire in <strong>1 hour</strong> for security reasons</li>
                        <li>Only use this link if you requested a password reset</li>
                        <li>Do not share this link with anyone else</li>
                    </ul>
                </div>
                
                <p>If you didn't request a password reset, please ignore this email. Your account remains secure.</p>
                <p>If you continue to have trouble, please contact our support team.</p>
            {{/if}}
        </div>
        
        <div class="footer">
            <p>© 2025 TripMaster. All rights reserved.</p>
            <p>{{#if isVietnamese}}Email này được gửi tự động, vui lòng không trả lời.{{else}}This is an automated email, please do not reply.{{/if}}</p>
        </div>
    </div>
</body>
</html>
